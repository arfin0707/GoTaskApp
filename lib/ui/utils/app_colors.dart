import 'package:flutter/material.dart';

class AppColors {
  // static const primary = Color(0xFF8A4FFF);
  static const primary = const Color(0xFF8566F2);
  static final primaryLight = const Color(0xFF8E7CFF);
  static const white = Colors.white;
  static const whiteTranparent = Colors.white24;
  static const textWhite70 = Colors.white70;
  static final grey50 = Colors.grey[100];
  static const Color tealGreen = Color(0xFF28B69E);  // Fresh teal green
  static const Color softPurple = Color(0xFFD480FC); // Light soft purple
  static const Color softRed = Color(0xFFFC8080);    // Gentle coral red
  static const Color warningLight = Color(0xFFFFB74D);  // Amber-orange (good contrast with white)
  static const Color dangerLight = Color(0xFFEF5350);   // Soft red (still readable with white)
  static  Color redShade = Colors.red.shade600;   // Soft red (still readable with white)
  static  Color redShade50 = Colors.red.shade50;   // Soft red (still readable with white)
  static  Color black_soft = Colors.black87 ;  // Soft red (still readable with white)
  static  Color black_12 = Colors.black12 ;  // Soft red (still readable with white)
  static  Color orange_shade50 = Colors.orange.shade50 ;  // Soft red (still readable with white)
  static  Color orange_shade700 = Colors.orange.shade700 ;  // Soft red (still readable with white)
  static  Color green_shade50 =Colors.green.shade50 ;  // Soft red (still readable with white)
  static  Color green_shade700 =Colors.green.shade700 ;  // Soft red (still readable with white)
  static  Color? grey800 = Colors.grey[800] ;  // Soft red (still readable with white)

}
